# Single combined App service (nginx SPA + Nest). Do not deploy apps/frontend or apps/backend separately.
[build]
builder = "DOCKERFILE"
dockerfilePath = "docker/app.Dockerfile"

[deploy]
restartPolicyType = "ON_FAILURE"
restartPolicyMaxRetries = 5
